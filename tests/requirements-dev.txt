# EarlyAlert — test-only dependencies
# Install alongside requirements.txt to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# Kept separate from requirements.txt so a production install doesn't
# pull in test tooling it will never use.

pytest>=8.0.0
pytest-mock>=3.12.0